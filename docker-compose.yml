name: projetofinalweb

# Inclui o stack da raiz do repositório (Postgres + pgAdmin + Keycloak).
# Na pasta DesenvWebApi: docker compose up -d
# Ou na raiz:            docker compose up -d

include:
  - path: ../docker-compose.yml
